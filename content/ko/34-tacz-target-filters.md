---
title: 타겟 규칙
slug: warfare-target-filters
order: 340
description: 총기 NPC의 타겟을 엔티티 ID, 팩션, 스코어보드 태그, 고급 스탠스 규칙으로 제한합니다.
product: dochi-warfare
category: 타겟
section: targets
status: Draft
version: 0.2.7
audience: 전투 설계자
tags:
  - target
  - filter
  - faction
---

## 타겟 규칙을 조심해야 하는 이유

총기 NPC는 대부분의 근접 NPC보다 먼 거리에서 공격할 수 있습니다. 그래서 잘못된 타겟 규칙은 바로 눈에 띕니다. 타겟 규칙은 작게 쌓으세요. 먼저 NPC가 발사하는지 확인하고, 그 다음 엔티티 ID, 태그, 고급 팩션 또는 스탠스 동작을 추가합니다.

## 엔티티 ID 타겟 목록

`Targets` 탭에서는 허용할 타겟 엔티티 ID를 선택할 수 있습니다. 목록에는 검색, 체크된 항목만 보기, 체크되지 않은 항목만 보기, 엔티티 타입 필터, 모드 ID 필터가 있습니다. 특정 몹 그룹만 공격하거나 특정 모드의 엔티티만 공격하게 만들 때 가장 명확한 방식입니다.

| 컨트롤 | 용도 |
| --- | --- |
| Search | `minecraft:zombie` 같은 레지스트리 ID를 검색합니다. |
| View filter | 전체, 체크됨, 체크 안 됨 항목을 나눠 봅니다. |
| Type filter | 넓은 living entity 그룹을 좁힙니다. |
| Mod filter | 선택한 모드 ID의 엔티티만 표시합니다. |
| Select all / none | 현재 필터로 보이는 목록에 일괄 적용합니다. |

타겟 엔티티 목록이 비어 있으면 엄격한 허용 목록을 사용하지 않습니다. 관리 중인 CustomNPCs 사수는 CustomNPCs NPC나 플레이어에 대한 실제 팩션 적대를 상속할 수 있도록 계속 후보를 탐색합니다. 후보는 명시적 선택 규칙, 같은 팩션 태그 예외, 실제 적대 팩션 규칙 중 하나에는 맞아야 하므로 수동적이거나 관계없는 엔티티가 자동 타겟이 되지는 않습니다.

긴 감지 거리에서는 공간 인덱스로 living target 후보를 좁힌 뒤 엔티티 ID, 태그, 팩션, 거리, 각도, 시야 규칙을 적용합니다. 0.2.7에서는 이미 전투 중이어도 새로 발견한 타겟마다 설정 거리·시야·인식 각도를 통과해야 합니다. 유효한 피격, 자기방어, 근거리 감지, 팩션 방어 반응만 문서화된 예외를 부여하며, 소리 없이 뒤에 있는 타겟이 전투 중이라는 이유만으로 자동 획득되지는 않습니다.

## 스코어보드 태그 규칙

태그 규칙은 엔티티 스코어보드 태그를 사용합니다.

| 필드 | 동작 |
| --- | --- |
| Required tags | 설정되어 있으면 타겟은 요구 태그 중 하나 이상을 가져야 합니다. |
| Rejected tags | 설정되어 있으면 거부 태그 중 하나라도 가진 타겟은 제외됩니다. |
| Attack same faction by tag | 요구 태그가 맞으면 같은 팩션 CustomNPCs도 공격할 수 있게 합니다. |

태그는 스크립트 이벤트에 특히 유용합니다. 예를 들어 던전 스크립트가 경보 페이즈 동안 플레이어나 NPC에게 `intruder` 태그를 붙이고, 전투가 끝나면 제거할 수 있습니다.

## 팩션과 Advanced 스탠스

Advanced 스탠스 규칙은 `Always`, NPC 체력 비율 이하, 타겟 팩션, 타겟 태그, 타겟 엔티티 ID를 조건으로 사용할 수 있습니다. `Always`와 NPC 체력 조건은 현재 전투 타겟이 없어도 판정됩니다. 타겟 팩션·태그·엔티티 조건은 살아 있는 타겟이 필요하므로 먼저 `Targets`에서 ID와 태그를 등록한 뒤 `Stance Mode: Advanced`에서 사용하세요.

Advanced 모드는 활성 조건을 하나만 지원합니다. 결과를 예측 가능하게 유지하기 위한 구조입니다. 조건이 맞지 않을 때 일반 `General` 설정으로 돌아가야 한다면 `Default` 동작을 사용하세요.

## 프로필 `Load`와 `Save As`

상단 바의 `Load`와 `Save As` 프로필은 아래 위치에 저장됩니다.

```text
config/dochi_warfare/target_entities/
```

현재 프로필은 타겟 엔티티 ID만 관리하는 파일이 아니라, NPC별 DW 총기 설정을 다른 CustomNPCs NPC에 적용하는 설정 프리셋입니다. 먼저 한 NPC에서 총기, AI, 타겟, 탄약, 풀 설정을 맞춘 뒤 `Save As`로 저장하고, 다른 NPC에서 `Load`하세요. 이전된 구버전 루트의 타겟 전용 JSON도 계속 불러올 수 있습니다.

## 실전 예시

| 상황 | 권장 규칙 |
| --- | --- |
| 좀비 경비 | 엔티티 목록에서 좀비 계열 엔티티 ID를 선택합니다. |
| 이벤트 페이즈 | `phase2_target` 요구 태그를 쓰고 페이즈 종료 시 제거합니다. |
| 같은 팩션 결투 | 이벤트 참여자에게만 요구 태그를 붙이고 같은 팩션 태그 공격을 켭니다. |
| 보스의 소환몹 처리 총기 | 소환몹 엔티티 ID만 허용하고 플레이어는 목록에서 제외합니다. |

## 타겟 규칙 디버깅

NPC가 발사하지 않으면 필터를 잠시 제거하고 기본 hostile 타겟으로 테스트하세요. 그 뒤 규칙을 하나씩 다시 추가합니다. 대부분의 타겟 문제는 엔티티 허용 목록, 요구 태그, 거부 태그, 같은 팩션 규칙을 기본 테스트 전에 한꺼번에 섞어서 생깁니다.

순찰 중 잠깐 엄폐한 살아 있는 타겟을 즉시 버리는 것도 정상 동작이 아닙니다. 관리 NPC는 짧은 전투 기억과 마지막 위치를 사용해 순간적인 시야 상실을 견딥니다. 다만 기억 시간 동안에도 최종 line-of-sight가 없으면 벽 너머로 직접 발사하지 않습니다.
